version: v1.0
name: Drumkit
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Drumkit
    task:
      jobs:
      - name: Compile drumkit
        commands:
          - checkout
          - autoreconf --install
          - ./configure
          - make

